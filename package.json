{
  "name": "@microsoft/fast-tooling-repository",
  "description": "A system of development tools, and utilities used à la carte or as a suite to build enterprise-grade websites and applications.",
  "version": "0.1.0",
  "author": {
    "name": "Microsoft",
    "url": "https://discord.gg/FcSNfg4"
  },
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/microsoft/fast-tooling.git"
  },
  "bugs": {
    "url": "https://github.com/microsoft/fast-tooling/issues/new/choose"
  },
  "scripts": {
    "test:diff:error": "echo \"Untracked files exist, try running npm prepare to identify the culprit.\" && exit 1",
    "test:diff": "git update-index --refresh && git diff-index --quiet HEAD -- || npm test:diff:error",
    "test": "npm run prettier:diff && npm run test:diff:error && npm run test --workspaces --if-present",
    "format:check": "npm run prettier:diff",
    "format": "npm run prettier"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx,js,html}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.12.13",
    "@babel/preset-env": "^7.12.13",
    "@types/jest": "^25.2.1",
    "@types/lodash-es": "^4.17.4",
    "@types/node": "^9.4.7",
    "chalk": "^2.4.2",
    "copyfiles": "^2.4.1",
    "dotenv": "^6.0.0",
    "glob": "^7.1.2",
    "husky": "^4.2.5",
    "lint-staged": "^10.1.2",
    "prettier": "2.0.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.4.0",
    "typescript": "^3.9.0",
    "yargs": "^16.2.0"
  },
  "dependencies": {}
}
